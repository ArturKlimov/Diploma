@model IEnumerable<Diploma.Models.Video>

@{
    Layout = "";
}
<div id="results">

    <table>
        <thead>
            <tr>
                <th>Заголовок</th>
                <th>Дата</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null)
            {
                foreach (var v in Model)
                {
                    <tr>
                        <td>@v.Title</td>
                        <td>@v.Date.ToShortDateString()</td>
                        <td>
                            @Html.ActionLink("Удалить", "DeleteVideo", "Admin", new { id = v.ID },
                                new { onclick = "return confirm('Вы уверены, что хотите удалить это видео?');" })
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>

</div>
@Scripts.Render("~/bundles/jquery")
<script type="text/javascript">
            $(document).on('keypress', (function (e) {
                if (e.which == 13) {
                    e.preventDefault();
                    var search = $('#search').val();
                    search = encodeURIComponent(search);
                    $('#results').load("/Admin/GetSearchVideos?search=" + search);
                }
            }));
</script>
